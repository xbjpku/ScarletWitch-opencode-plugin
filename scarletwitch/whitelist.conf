# Permission configuration file
#
# Two sections:
#   [write]  — whitelist: only listed paths are writable (default: deny write)
#   [read]   — blacklist: listed paths are NOT readable (default: allow read)
#
# Each line under a section is a path prefix.

[write]
/home/user/project/
/tmp/
/mnt/user-ssd/xiaobangjun/pku_workspace/test.txt

[read]
/mnt/user-ssd/zhaoyihao1
